<%= form_for(@advtype) do |f| %>
  <% if @advtype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@advtype.errors.count, "error") %> prohibited this advtype from being saved:</h2>

      <ul>
      <% @advtype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
